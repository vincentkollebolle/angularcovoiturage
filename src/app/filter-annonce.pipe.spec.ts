import { FilterAnnoncePipe } from './filter-annonce.pipe';

describe('FilterAnnoncePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnoncePipe();
    expect(pipe).toBeTruthy();
  });
});
